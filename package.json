{
  "name": "program-specification-language",
  "version": "0.3.0",
  "description": "Declarative DSL for structured resistance training programs.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "AI_CONTEXT.md",
    "docs",
    "examples"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "cli:build": "tsc -p tsconfig.cli.json",
    "psl": "node dist-cli/cli/src/main.js",
    "psl:dev": "npm run cli:build && node dist-cli/cli/src/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "format": "prettier --check .",
    "test": "npm run test:compile && node .tmp/testdist/tests/run.js",
    "test:compile": "tsc -p tsconfig.json --outDir .tmp/testdist",
    "verify:package": "node --input-type=module -e \"import { mkdir, rm } from 'node:fs/promises'; await mkdir('.tmp', { recursive: true }); await rm('dist', { recursive: true, force: true });\" && npm pack --cache ./.tmp/npm-cache --json > .tmp/pack.json && node scripts/verify-package.mjs && node --input-type=module -e \"import { rm, writeFile } from 'node:fs/promises'; await rm('.tmp/package-smoke/node_modules', { recursive: true, force: true }); await rm('.tmp/package-smoke/package-lock.json', { force: true }); await writeFile('.tmp/package-smoke/package.json', JSON.stringify({ name: 'package-smoke', private: true, version: '0.0.0', type: 'module', overrides: { yaml: 'file:../../node_modules/yaml' } }, null, 2));\" && npm --cache ./.tmp/npm-cache --prefix .tmp/package-smoke install --no-package-lock --no-save ./.tmp/package-smoke/package.tgz && cd .tmp/package-smoke && node --input-type=module -e \"await import('program-specification-language')\"",
    "test:vitest": "vitest run",
    "test:vitest:watch": "vitest",
    "test:watch": "npm run test:vitest:watch"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "eslint": "^9.20.1",
    "prettier": "^3.5.1",
    "typescript": "^5.8.2",
    "vitest": "^3.0.5"
  }
}
